import { CircularProgress, LinearProgress } from '@mui/material';

const Loading = () => (
      <LinearProgress sx={{
        width: '100%',
        margin: 10
      }}/>
  );

export default Loading;
